spring:
  flyway:
    enabled: true
  jpa:
    hibernate:
      ddl-auto: validate
  kafka:
    listener:
      auto-startup: false

identity:
  jwt:
    issuer: identity
    audience: hospital-management
    access-token-ttl: PT15M
